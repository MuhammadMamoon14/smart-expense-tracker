# smart-expense-tracker
A full-stack expense tracking application built with FastAPI (Python) and a sleek dark dashboard UI. Features JWT authentication, expense/income/savings/bill management, built-in analytics (mean, median, mode, category breakdown, monthly trends), budget alerts, and CSV export — all backed by local JSON files with zero database setup required.
